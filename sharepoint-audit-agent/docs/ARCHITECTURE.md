
Architecture

Claude/Gemini act as interactive UX; local PowerShell + Python do privileged work:
LLM -> local shell -> PnP.PowerShell (wrapper -> original script) -> JSON -> Python -> Markdown/HTML.
